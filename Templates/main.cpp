#include<bits/stdc++.h>
#define rep(i,a,b)  for(int i=a;(a<b)?i<b:i>b;(a<b)?i++:i--)
#define ll long long
using namespace std;

void solve(){
	
}

int32_t main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int T=1;
	cin >> T;

	while(T--){
		solve();
	}
}